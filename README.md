Syfe-assignment
├── cmd
│   └── server
│       └── main.py            # Entry point of the application
├── internal
│   ├── config
│   │   └── config.py          # Configuration loading & struct
│   └── server
│       └── server.py          # Server class & lifecycle management
├── pkg
│   └── api
│       └── handlers.py        # API endpoint handlers
├── configs
│   └── config.yaml            # Configuration file (same as Go)
├── migrations
│   └── 0001_init.up.sql       # Same migration file
├── requirements.txt            # Python dependencies
├── Dockerfile                 # Python-based Dockerfile
├── Makefile                   # Python tasks
├── .gitignore
└── README.md
